# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
*.egg

# Virtual environments
.venv/
venv/
env/

# IDE
.idea/
.vscode/
*.swp
*.swo
.claude/

# Experiment outputs (raw per-run data, too large for git)
results/runs/

# Keep aggregated tables and figures (small, needed for paper)
# results/tables/ and results/figures/ are NOT ignored

# Data and checkpoints (downloaded via scripts)
# Note: data/ispd_placements.pkl is tracked via git add -f (small, precious)
# Note: checkpoints/neural_vsr/*.pt are tracked via git add -f (small, our work)
data/*
!data/ispd_placements.pkl
checkpoints/*
!checkpoints/neural_vsr/
*.pth
*.safetensors
local_artifacts/
third_party/chipdiffusion/benchmarks/
third_party/chipdiffusion/datasets/graph/ispd2005/
third_party/chipdiffusion/data-gen/outputs/

# OS
.DS_Store
Thumbs.db

# Wandb
wandb/

# Jupyter
.ipynb_checkpoints/

# Coverage
.coverage
htmlcov/
